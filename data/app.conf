server {
    listen 80;
    server_name mgrs_app;
    server_tokens off;

    location / {
        return 301 https://$host$request_uri;
    root /var/www/mgrs;
    index index.html;
    
    location / {
      try_files $uri $uri/ =404;
    }
}

